#ifndef FOO_HPP
#define FOO_HPP

/// Dummy class that does nothing.
struct foo {

    /// Return the argument that was passed in.
    int value(int i             ///< [in] value to forward
        );
};

#endif
